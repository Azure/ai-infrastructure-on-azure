# Generation config: Grace Blackwell (GB200 / GB300) — Azure NDv6 — 4 GPUs per node
# Source this file before launching GPU GEMM tests.

GPUS_PER_NODE=4
UBERGEMM_PATH="/usr/libexec/datacenter-gpu-manager-4/plugins/cuda13/updated/ubergemm"
TEST_DURATION=60
