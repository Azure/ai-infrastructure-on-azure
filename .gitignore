*.out
*.err
github_conf/
github_conf/branch_protection_rules.json

# The dashboards are downloaded when the deploy aks script runs. The dashboards
# may be updated on the Github repos, so not checking them in.
infrastructure_references/aks/configs/monitoring/
node_modules
